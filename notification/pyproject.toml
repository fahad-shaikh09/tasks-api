[project]
name = "tasks-notification"
version = "0.1.0"
description = "Notification microservice for Tasks API - subscribes to events via Dapr"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "sqlmodel>=0.0.31",
    "psycopg2-binary>=2.9.11",
]

[tool.uv]
dev-dependencies = []
